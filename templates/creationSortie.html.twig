{% extends "base.html.twig" %}

{% block main %}

    <h1 class="text-center my-4">Créer une sortie</h1>

    <div class="container mb-5">
        <div class="col-8 mx-auto">
            {{ form_start(formSortie) }}

            {{ form_widget(formSortie) }}
            <input type="hidden" name="_token" value="{{ csrf_token('_token') }}"/>
            <div class="d-flex">
                <div class="me-2">
                    <button type="submit" class="btn btn-primary ">Enregistrer</button>
                </div>
                <div class="me-2">
                    <a href="{{ path("default_accueil") }}"
                       class="btn btn-outline-primary">Annuler</a>
                </div>
            </div>
            {{ form_end(formSortie) }}
        </div>
    </div>

{% endblock %}